<% content_for :title, "Categories" %>

<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
  <% end %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Categories</h1>
    <%= link_to "New category", new_admin_category_path, class: "rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white block font-medium" %>
  </div>

</div>

<div class="flex flex-col w-full">
  <div class="overflow-x-auto sm:mx-0.5 lg:mx-0.5">
    <div class="py-2 inline-block min-w-full">
      <div class="overlow-x-auto">
        <table class="min-w-full overflow-x-scroll">
          <thead class="bg-white border-b">
            <tr>
              <th scope="col" class='text-sm font-medium text-gray-800 px-6 py-4 text-left'>
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left">
                Name 
              </th>
              <th scope="col" class="text-sm font-medium text-gray-800 px-6 py-4 text-left">
                Description 
              </th>
            </tr>
          </thead>
          <tbody>
            <% @admin_categories.each do |admin_category| %>
              <tr class="bg-gray-50 border-b">
                <td class="px-6 py-4 whitespace-nowrap font-medium text-gray-800 underline">
                  <%#Checks if there is an image present in the category and if there is then loads it into the table otherwise uses a placeholder#%>
                  <%= admin_category.image.present? ? image_tag(admin_category.image.variant(:thumb)) : image_tag(%"https://placehold.co/50")%>
                </td>
                <td class="px-6 py-4 whitespace-nowrap font-medium text-gray-800 underline">
                  <%= link_to admin_category.name, edit_admin_category_path(admin_category) %>
                </td>
                <td class="px-6 py-4 whitespace-nowrap font-medium text-gray-800">
                  <%= admin_category.description  %>
                </td>
              </tr>
            <% end %>
        </table>
      </div>
    </div>
  </div>
</div>
